mixin button(props)
    - const { label, type, theme = "blue", size = "md", disabled = false } = props;
    - const themeVariant = ` button--${theme}`;
    - const sizeVaraint = ` button--${size}`;

    button(class=`button${themeVariant}${sizeVaraint}`, disabled=disabled, type=type)
        .row.no-gutters
            if label
                .col.button__label #{label}
